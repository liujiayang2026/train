# Question 1

The numerical model is implemented in `code/solve_question1.py`.

It treats the 1200 kg ballast ball as a separate concentrated load at the
barrel-chain connection and assumes that the buoy remains upright because of
its hydrostatic stability. Run the script to generate the summary and chain
profile CSV files in `results/`.
