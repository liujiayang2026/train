# Question 1

This folder contains the preliminary analysis for Project 4, Question 1.

Run from the workspace root:

```powershell
& 'C:\Users\lenovo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' 'project4\question-1\code\run_question1.py'
```

The script discovers the original workbook under `project4`, keeps it read-only,
and writes cleaned data, statistical tables, SVG figures, and a short report into
the categorized subfolders under `question-1`.
