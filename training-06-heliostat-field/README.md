# Human modeling intake

This package is created and maintained by the `train` repository. Humans and modeling Agents provide only `source/` and `process/`; do not create or curate `final/`.

Read `PROCESS_GUIDE.md` before adding materials. Before handoff, classify every file in `process/_staging/` and run the readiness validator from the train repository.
