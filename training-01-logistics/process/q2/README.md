# Q2 process index

- Question requirement:
- Current candidate routes:
- Known conflicts:
- Explicit human decisions:

Create one `routes/rNN-short-name/` directory per materially different route. Keep each route's model, code, runs, results, figures, validation, and logs together.

## Legacy intake pointers

- Candidate materials: `process/legacy-package/q2_revised/`, `process/legacy-package/问题2/`, `process/legacy-package/问题2_优化版/`.
- Integrated implementation: `process/legacy-package/integrated_code/problem2_complete.py`.
- Multiple versions exist; no route is marked adopted at intake stage.
