# Route r05-shifted-hex-optical-pruning

- 对应问题：q2
- 过程状态：candidate
- 要回答的内容：在六角点阵解析可行性的基础上，优化统一镜面尺寸、塔位、点阵旋转和平移，并通过低贡献镜面筛除与局部移动提高单位面积年平均输出热功率。
- 输入与数据口径：沿用 q1/r01 的 60 时点、锥形太阳光束、圆柱集热器和效率定义；以 r03 的 256 光线结果作为正式对照。
- 核心模型/算法：带安全净距的六角点阵增加旋转角和基本单元内二维偏移；代理批量初筛、代表时点真实光线筛选和完整年真实光线逐级复核；对功率有裕量的方案删除低贡献镜面，再对少量低效镜面做受约束局部移动。
- 关键公式位置：完整推导、优化流程、锥形光线评价、验证和最终结果见 `model/question2-complete-solution.md`；简化布局定义见 `model/shifted-hex-pruning-model.md`。
- 代码入口：`code/run_shifted_hex_experiment.py`；几何函数位于 `code/shifted_hex_model.py`。
- 关联运行：`runs/run-20260817-1534-focused-search/` 暴露代理排序偏向低功率高效率方案；`runs/run-20260817-1544-power-feasible-search/` 暴露代表时点预门槛过高后的错误回退；`runs/run-20260817-1548-direct-power-ranking/` 定位 32 光线高效率边界；`runs/run-20260817-1556-targeted-real-ray-refinement/` 发现该边界在 64 光线下不可行；`runs/run-20260817-1606-square-region-multifidelity/` 转入近方形镜面区域并以 64/128 光线筛选；`runs/run-20260817-1726-spatial-power-zones-v2/` 对最终镜场执行 256 光线逐镜功率复算和 60 分区空间诊断。
- 与其他问题/路线的关系：保留 r03 的六角点阵结构，修正其固定相位、固定平移和候选复核不足；不沿用已放弃 r04 的径向环生成器。
- 当前优点：几何约束解析稳定；最终 `6.3 x 6.3 m` 方案在 32/64/128/256 光线下均满足 60 MW，并将 256 光线单位面积功率提高到 `0.48043 kW/m2`。
- 已知缺陷或冲突：属于受限参数化布局，不能证明任意自由镜位下的全局最优；最终功率裕量为约 `0.408 MW`，仍应在正式采用前保留高精度结果说明。
- 放弃时的原因：不适用。
