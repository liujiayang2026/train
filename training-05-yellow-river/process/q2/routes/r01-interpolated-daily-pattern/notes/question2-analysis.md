# 问题二路线级分析索引

本文件只维护路线级解释，不复制某次运行的机器结果。当前规范复现与完整分析摘要位于：

```text
../runs/run-20260809-2036-time-block-q1/results/reports/question2-analysis.md
```

对应机器表、图和稳健性证据均在同一 run 的 `results/`、`figures/`、`validation/` 与 `logs/` 中。历史导入结果位于 `run-20260809-0000-imported-legacy/`，状态为 `partial`，不得用其旧路径说明替代本次运行记录。

## 当前保守解释框架

1. 年际和月份结论以基准小时线性插值口径为主，同时报告另外两种插值方案的总量、月份占比和事件重合度。
2. 突变结果称为“候选事件”，并结合 27 组窗口、阈值与最小间隔的敏感性结果；无外部运行记录时不作调度原因归因。
3. 周期结论同时检查两段连续五年子样本；六年数据只能支持年尺度成分，不能证明长期稳定周期。
4. 沙通量沿用问题一模型补全序列，必须同步披露补全比例及其尚未完成端到端误差传播的限制。
5. 不同量纲的绝对流量和绝对沙通量分图展示；归一化对比仅用于形状比较。

路线仍为 `candidate`。本次复现和补验证不构成人工 adopted 决定。
