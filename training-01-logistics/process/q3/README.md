# Q3 process index

- Question requirement:
- Current candidate routes:
- Known conflicts:
- Explicit human decisions:

Create one `routes/rNN-short-name/` directory per materially different route. Keep each route's model, code, runs, results, figures, validation, and logs together.

## Legacy intake pointers

- Candidate materials: `process/legacy-package/q3_revised/`.
- Integrated implementation: `process/legacy-package/integrated_code/problem3_complete.py`.
- The legacy folder contains multiple algorithms and result sets that require route comparison.
